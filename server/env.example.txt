# Copy this file to ".env" in the same folder and fill in real values.
# ".env" is the file your app actually reads. ".env.example" is just a template.

# Where MongoDB is running. If you installed MongoDB locally with default
# settings, this value already works and you do not need to change it.
MONGO_URI=mongodb://127.0.0.1:27017/sema_serikali

# The port your Express server will run on.
PORT=5000

# Your free Gemma 4 API key from Google AI Studio (https://aistudio.google.com).
# See README.md "Getting a Gemma 4 API key" for the exact steps.
GOOGLE_API_KEY=your_api_key_here

# The exact Gemma 4 model to call through the Gemini API.
# gemma-4-26b-a4b-it = lower latency, best for a live demo.
# gemma-4-31b-it     = larger dense model, slightly stronger, a bit slower.
GEMMA_MODEL=gemma-4-26b-a4b-it
